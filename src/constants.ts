// Rate refresh interval (ms)
export const AUTO_REFRESH_INTERVAL = 60000;

// Toast auto-dismiss time (ms)
export const TOAST_DURATION = 4500;

// API retry configuration
export const API_RETRY_ATTEMPTS = 3;
export const API_RETRY_DELAY = 1500;

// Spread analysis thresholds (%)
export const SPREAD_HEALTHY_THRESHOLD = 2.0;
export const SPREAD_MODERATE_THRESHOLD = 6.0;
export const SPREAD_MAX_SCALE = 10;

// Cache TTL (ms)
export const CACHE_TTL = 300000;

// Fallback rates
export const FALLBACK_RATES = {
  bcvUsd: 685.94,
  bcvEur: 783.78,
  binance: 817.00,
};
